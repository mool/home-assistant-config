---
id: motion_detected_office_alarmed
alias: Motion Detected In The Office Alarmed
trigger:
  - platform: state
    entity_id: binary_sensor.motion_sensor_office
    to: 'on'
condition:
  condition: or
  conditions:
    - condition: state
      entity_id: input_boolean.away
      state: 'on'
    - condition: state
      entity_id: input_boolean.bedtime
      state: 'on'
action:
  - service: notify.mobile_app_iphone_de_pablo
    data:
      title: 'Security - Office'
      message: 'Motion detected in the Office'
      data:
        push:
          sound:
            name: US-EN-Morgan-Freeman-Motion-Detected.wav
            critical: 1
            volume: 1.0
  - service: homeassistant.turn_on
    entity_id: group.inside_lights
